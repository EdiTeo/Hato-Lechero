import {StyleSheet} from 'react-native';
//es solo para estilos
export const barraLatera = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
      },
});